test:
  override:
    - go test -v ./...
    - echo $CIRCLE_ARTIFACTS
    - ls $CIRCLE_ARTIFACTS

deployment:
  production:
    branch: master
    commands:
      - echo $CIRCLE_ARTIFACTS
      - ls $CIRCLE_ARTIFACTS