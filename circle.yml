test:
  override:
    - go test -v ./...
    - echo $CIRCLE_ARTIFACTS
    - touch $CIRCLE_ARTIFACTS/foo.txt

deployment:
  production:
    branch: master
    commands:
      - echo $CIRCLE_ARTIFACTS
      - ls $CIRCLE_ARTIFACTS